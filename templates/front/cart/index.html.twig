{% extends 'front/layout.html.twig' %}

{% block content %}
    <h1 class="mb-4">Votre panier</h1>

    {% if items is empty %}
        <div class="alert alert-info">Votre panier est vide.</div>
    {% else %}
        <div class="table-responsive">
            <table class="table align-middle">
                <thead>
                <tr>
                    <th>Produit</th>
                    <th class="text-center">Qté</th>
                    <th class="text-end">Prix</th>
                    <th class="text-end">Sous-total</th>
                    <th></th>
                </tr>
                </thead>
                <tbody>
                {% for row in items %}
                    <tr>
                        <td>{{ row.product.name }}</td>
                        <td class="text-center" style="width: 150px;">
                            <form action="{{ path('app_cart_set', {id: row.product.id}) }}" method="post" class="d-flex gap-2">
                                <input type="number" min="1" name="qty" value="{{ row.quantity }}" class="form-control form-control-sm" />
                                <button class="btn btn-sm btn-outline-primary">OK</button>
                            </form>
                        </td>
                        <td class="text-end">{{ row.product.price|number_format(2, ',', ' ') }} €</td>
                        <td class="text-end">{{ row.subtotal|number_format(2, ',', ' ') }} €</td>
                        <td class="text-end">
                            <a href="{{ path('app_cart_remove', {id: row.product.id}) }}" class="btn btn-sm btn-outline-danger">Supprimer</a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
                <tfoot>
                <tr>
                    <th colspan="3" class="text-end">Total</th>
                    <th class="text-end">{{ total|number_format(2, ',', ' ') }} €</th>
                    <th></th>
                </tr>
                </tfoot>
            </table>
        </div>

        <a href="{{ path('app_cart_clear') }}" class="btn btn-outline-secondary">Vider le panier</a>
    {% endif %}
{% endblock %}